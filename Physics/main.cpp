#include <iostream>
#include <vector>
#include <time.h>
#include <stdlib.h>

void main() {
	std::cout << "Loading application :)" << std::endl;
	srand(time(NULL));
		
	std::cout << "Exiting application :)" << std::endl;
	std::getchar();
}